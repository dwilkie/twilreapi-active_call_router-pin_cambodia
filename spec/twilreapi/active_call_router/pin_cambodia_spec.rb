require 'spec_helper'

describe Twilreapi::ActiveCallRouter::PinCambodia do
  it 'has a version number' do
    expect(Twilreapi::ActiveCallRouter::PinCambodia::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
